-- OS_Installed.lua
-- Liste des systèmes installés (modifiable)

return {
    -- Exemple :
    -- { name = "Windows11OS", path = "/os/w11os/startup.lua" },
    -- { name = "LinuxOS", path = "/os/linux/startup.lua" }
}
